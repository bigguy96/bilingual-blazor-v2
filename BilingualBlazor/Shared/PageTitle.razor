@inject State.HeadState Head

@code{
    [Parameter]
    public string Title { get; set; }

    protected override void OnInitialized()
    {
        Head.SetTitle(Title);

        //base.OnInitialized();
    }
}